<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.puhon_sample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Puhon_sample">
        <activity android:name=".retrieveFeelings"></activity>
        <activity android:name=".retrieveMood" />
        <activity android:name=".questions" />
        <activity android:name=".question_2" />
        <activity android:name=".feelings" />
        <activity android:name=".settings" />
        <activity android:name=".choices" />
        <activity android:name=".moods" />
        <activity android:name=".introduction" />
        <activity android:name=".menu" />
        <activity android:name=".login" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>